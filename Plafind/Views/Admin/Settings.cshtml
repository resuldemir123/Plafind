@model SiteSettingsViewModel
@{
    ViewData["Title"] = "Site Ayarları";
}

<div class="page-header">
    <h1>🛠 Site Ayarları</h1>
    <p>Genel site ayarlarını yönetin</p>
</div>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0"><i class="fas fa-cog me-2"></i>Ayarlar</h5>
            </div>
            <div class="card-body">
                @if (TempData["Success"] != null)
                {
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <i class="fas fa-check-circle me-2"></i>@TempData["Success"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }

                <form asp-action="Settings" method="post">
                    <div class="mb-3">
                        <label asp-for="SiteTitle" class="form-label">Site Başlığı</label>
                        <input asp-for="SiteTitle" class="form-control" />
                        <span asp-validation-for="SiteTitle" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="SiteDescription" class="form-label">Site Açıklaması</label>
                        <textarea asp-for="SiteDescription" class="form-control" rows="3"></textarea>
                        <span asp-validation-for="SiteDescription" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="ContactEmail" class="form-label">İletişim E-postası</label>
                        <input asp-for="ContactEmail" type="email" class="form-control" />
                        <span asp-validation-for="ContactEmail" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="ContactPhone" class="form-label">İletişim Telefonu</label>
                        <input asp-for="ContactPhone" class="form-control" />
                        <span asp-validation-for="ContactPhone" class="text-danger"></span>
                    </div>

                    <div class="mb-3 form-check">
                        <input asp-for="MaintenanceMode" type="checkbox" class="form-check-input" />
                        <label asp-for="MaintenanceMode" class="form-check-label">Bakım Modu</label>
                    </div>

                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save me-2"></i>Kaydet
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

