@model IEnumerable<AdminLog>
@{
    ViewData["Title"] = "Sistem Logları";
    var adminUsers = ViewBag.AdminUsers as Dictionary<string, ApplicationUser?>;
}

<div class="page-header">
    <h1>📝 Sistem Logları</h1>
    <p>Tüm sistem loglarını görüntüleyin</p>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-body">
        @if (Model != null && Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Tarih</th>
                            <th>Admin</th>
                            <th>İşlem</th>
                            <th>Varlık Tipi</th>
                            <th>Açıklama</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var log in Model)
                        {
                            <tr>
                                <td>@log.CreatedDate.ToString("dd.MM.yyyy HH:mm")</td>
                                <td>
                                    @if (adminUsers != null && !string.IsNullOrEmpty(log.AdminUserId) && adminUsers.ContainsKey(log.AdminUserId))
                                    {
                                        @adminUsers[log.AdminUserId]?.UserName
                                    }
                                    else
                                    {
                                        <span class="text-muted">Bilinmiyor</span>
                                    }
                                </td>
                                <td><span class="badge bg-primary">@log.Action</span></td>
                                <td>@log.EntityType</td>
                                <td>@log.Description</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center py-5">
                <i class="fas fa-list-alt fa-3x text-muted mb-3"></i>
                <p class="text-muted">Log kaydı bulunmamaktadır.</p>
            </div>
        }
    </div>
</div>

