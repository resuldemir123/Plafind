@model IEnumerable<UserRoleViewModel>
@{
    ViewData["Title"] = "Rol Atama";
    var allRoles = ViewBag.AllRoles as List<Microsoft.AspNetCore.Identity.IdentityRole> ?? new List<Microsoft.AspNetCore.Identity.IdentityRole>();
}

<div class="page-header">
    <h1>🛡 Rol Atama</h1>
    <p>Kullanıcılara rol atayın veya kaldırın</p>
</div>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="fas fa-check-circle me-2"></i>@TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="card border-0 shadow-sm">
    <div class="card-body">
        @if (Model != null && Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Kullanıcı Adı</th>
                            <th>E-posta</th>
                            <th>Mevcut Roller</th>
                            <th>İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td><strong>@user.UserName</strong></td>
                                <td>@user.Email</td>
                                <td>
                                    @foreach (var role in user.Roles)
                                    {
                                        <span class="badge bg-primary me-1">@role</span>
                                    }
                                    @if (!user.Roles.Any())
                                    {
                                        <span class="text-muted">Rol yok</span>
                                    }
                                </td>
                                <td>
                                    <div class="dropdown">
                                        <button class="btn btn-sm btn-primary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                            <i class="fas fa-cog me-1"></i>İşlemler
                                        </button>
                                        <ul class="dropdown-menu">
                                            @foreach (var role in allRoles ?? Enumerable.Empty<Microsoft.AspNetCore.Identity.IdentityRole>())
                                            {
                                                @if (!string.IsNullOrEmpty(role.Name))
                                                {
                                                    @if (!user.Roles.Contains(role.Name))
                                                    {
                                                        <li>
                                                            <form asp-action="AssignRole" method="post" style="display:inline;">
                                                                <input type="hidden" name="userId" value="@user.UserId" />
                                                                <input type="hidden" name="roleName" value="@role.Name" />
                                                                <button type="submit" class="dropdown-item">
                                                                    <i class="fas fa-plus me-2"></i>@role.Name Ekle
                                                                </button>
                                                            </form>
                                                        </li>
                                                    }
                                                    else
                                                    {
                                                        <li>
                                                            <form asp-action="RemoveRole" method="post" style="display:inline;">
                                                                <input type="hidden" name="userId" value="@user.UserId" />
                                                                <input type="hidden" name="roleName" value="@role.Name" />
                                                                <button type="submit" class="dropdown-item text-danger" onclick="return confirm('Bu rolü kaldırmak istediğinize emin misiniz?');">
                                                                    <i class="fas fa-times me-2"></i>@role.Name Kaldır
                                                                </button>
                                                            </form>
                                                        </li>
                                                    }
                                                }
                                            }
                                        </ul>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center py-5">
                <i class="fas fa-users fa-3x text-muted mb-3"></i>
                <p class="text-muted">Kullanıcı bulunmamaktadır.</p>
            </div>
        }
    </div>
</div>

