@model IEnumerable<ContactMessage>
@{
    ViewData["Title"] = "Kullanıcı Mesajları";
}

<div class="page-header">
    <h1>💬 Kullanıcı Mesajları</h1>
    <p>Kullanıcılardan gelen mesajları görüntüleyin</p>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-body">
        @if (Model != null && Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Tarih</th>
                            <th>Ad Soyad</th>
                            <th>E-posta</th>
                            <th>Konu</th>
                            <th>Durum</th>
                            <th>İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var message in Model)
                        {
                            <tr>
                                <td>@message.CreatedDate.ToString("dd.MM.yyyy HH:mm")</td>
                                <td>@message.Name</td>
                                <td>@message.Email</td>
                                <td>@message.Subject</td>
                                <td>
                                    @if (message.IsRead)
                                    {
                                        <span class="badge bg-success">Okundu</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-warning">Okunmadı</span>
                                    }
                                </td>
                                <td>
                                    <button class="btn btn-sm btn-primary" data-bs-toggle="modal" data-bs-target="#messageModal@message.Id">
                                        <i class="fas fa-eye"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center py-5">
                <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                <p class="text-muted">Mesaj bulunmamaktadır.</p>
            </div>
        }
    </div>
</div>

