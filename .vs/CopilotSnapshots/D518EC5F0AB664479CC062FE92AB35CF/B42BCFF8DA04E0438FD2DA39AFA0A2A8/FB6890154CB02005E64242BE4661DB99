@model LoginViewModel
@{
    ViewData["Title"] = "Giriş Yap";
    Layout = "_Layout";
}

<style>
    .auth-page {
        min-height: calc(100vh - 80px);
        background: linear-gradient(135deg, #2b2f42 0%, #1b2030 100%); /* dark gradient to remove white */
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 40px 0; /* keep below navbar */
        position: relative;
        margin: 0; /* remove negative margins that caused white edges */
        overflow: hidden;
    }

    .auth-page::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: radial-gradient(circle at 20% 20%, rgba(255, 255, 255, 0.03) 0%, transparent 50%), 
                    radial-gradient(circle at 80% 80%, rgba(255, 255, 255, 0.02) 0%, transparent 50%);
        animation: backgroundShift 20s ease-in-out infinite;
        z-index: 0;
    }

    @@keyframes backgroundShift {
        0%, 100% { opacity: 0.6; }
        50% { opacity: 0.8; }
    }

    .auth-card {
        background: rgba(10, 14, 22, 0.7); /* dark translucent card */
        backdrop-filter: blur(8px);
        border-radius: 16px;
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.6);
        padding: 40px 36px;
        max-width: 520px;
        width: 90%;
        position: relative;
        z-index: 1;
        animation: slideUp 0.6s ease-out;
        color: #e6eef8; /* light text */
    }

    @@keyframes slideUp {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }

    .auth-header {
        text-align: center;
        margin-bottom: 28px;
    }

    .auth-icon {
        width: 64px;
        height: 64px;
        margin: 0 auto 14px;
        background: linear-gradient(135deg, #667eea, #764ba2);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .auth-title {
        font-size: 24px;
        font-weight: 700;
        color: #f1f5f9;
        margin-bottom: 6px;
    }

    .auth-subtitle {
        color: #a8b3c7;
        font-size: 13px;
    }

    .auth-form-group {
        margin-bottom: 18px;
    }

    .auth-form-group label {
        display: block;
        margin-bottom: 8px;
        color: #cbd5e1;
        font-weight: 600;
        font-size: 13px;
    }

    .auth-form-control {
        width: 100%;
        padding: 12px 14px;
        border-radius: 8px;
        border: 1px solid rgba(255, 255, 255, 0.06);
        background: rgba(255, 255, 255, 0.03);
        color: #e6eef8;
        box-sizing: border-box;
    }

    .auth-form-control:focus {
        box-shadow: 0 0 4px rgba(102, 126, 234, 0.06);
        outline: none;
    }

    .auth-btn-primary {
        width: 100%;
        padding: 12px;
        background: linear-gradient(135deg, #667eea, #764ba2);
        border: none;
        border-radius: 8px;
        color: white;
        font-weight: 600;
    }

    .auth-text-danger {
        color: #ffb4b4;
        font-size: 13px;
        margin-top: 6px;
        display: block;
    }

    .auth-validation-summary {
        background: rgba(255, 20, 20, 0.06);
        border-left: 4px solid rgba(229, 62, 62, 0.9);
        padding: 12px 16px;
        border-radius: 6px;
        margin-top: 14px;
    }

    .auth-footer {
        text-align: center;
        margin-top: 18px;
        padding-top: 18px;
        border-top: 1px solid rgba(255, 255, 255, 0.04);
    }

    .auth-footer a {
        color: #9fb1ff;
    }

    .forgot-password a {
        color: #9fb1ff;
    }

    /* Responsive */
    @@media (max-width: 576px) {
        .auth-card {
            padding: 24px;
            width: 94%;
        }

        .auth-title {
            font-size: 20px;
        }
    }
</style>

<div class="auth-page">
    <div class="auth-card">
        <div class="auth-header">
            <div class="auth-icon">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
                    <path fill="white" d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm0 14.2c-2.5 0-4.71-1.28-6-3.22.03-1.99 4-3.08 6-3.08 1.99 0 5.97 1.09 6 3.08-1.29 1.94-3.5 3.22-6 3.22z" />
                </svg>
            </div>
            <h2 class="auth-title">Hoş Geldiniz</h2>
            <p class="auth-subtitle">Alanya İşletme Rehberi'ne giriş yapın</p>
        </div>

        <form asp-action="Login" method="post">
            <div class="auth-form-group">
                <label asp-for="Email">E-posta</label>
                <input asp-for="Email" class="auth-form-control" placeholder="ornek@email.com" />
                <span asp-validation-for="Email" class="auth-text-danger"></span>
            </div>

            <div class="auth-form-group">
                <label asp-for="Password">Şifre</label>
                <input asp-for="Password" type="password" class="auth-form-control" placeholder="••••••••" />
                <span asp-validation-for="Password" class="auth-text-danger"></span>
                <div class="forgot-password">
                    <a href="#">Şifremi Unuttum?</a>
                </div>
            </div>

            <div class="auth-form-group">
                <button type="submit" class="auth-btn-primary">Giriş Yap</button>
            </div>

            <div class="auth-text-danger">
                @Html.ValidationSummary(false, "", new { @class = "auth-validation-summary" })
            </div>
        </form>

        <div class="auth-footer">
            <p style="color: #a8b3c7; font-size: 14px; margin: 0;">
                Hesabınız yok mu? <a asp-controller="Account" asp-action="Register">Kayıt Olun</a>
            </p>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}